# What I've Learned

A running log of concepts and patterns I've internalized.

## 2026-04-30 — Session 0: Setup
- Next.js is a React framework for building production web apps
- TypeScript adds types to JavaScript, catching errors before runtime
- Tailwind CSS uses utility classes (e.g., `bg-black text-white`) for fast styling
- Git uses SSH keys for authentication — more secure than passwords
- A single Mac can have multiple GitHub identities via ~/.ssh/config and ~/.gitconfig includeIf
